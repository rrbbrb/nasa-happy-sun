import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SunInfoScreen = (props) => {
    return <View>
        <Text>SunInfoScreen</Text>
    </View>
};

const styles = StyleSheet.create({});

export default SunInfoScreen;